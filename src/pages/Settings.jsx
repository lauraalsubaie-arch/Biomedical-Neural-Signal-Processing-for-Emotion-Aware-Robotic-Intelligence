import { useState } from "react";

export default function Settings({ user }) {
  const [saved,setSaved]=useState(false);
  return <div><div className="page-title-block"><h2>Settings</h2><p>Security, notifications, data consent, and integration preferences.</p></div><div className="settings-grid"><div className="card"><h3>Security</h3><label><input type="checkbox" defaultChecked/> Require re-authentication before editing medical reports</label><label><input type="checkbox" defaultChecked/> Enable audit logging</label><label><input type="checkbox"/> Two-factor authentication</label></div><div className="card"><h3>Notifications</h3><label><input type="checkbox" defaultChecked/> High-risk sensor alerts</label><label><input type="checkbox" defaultChecked/> New messages</label><label><input type="checkbox" defaultChecked/> Appointment updates</label><label><input type="checkbox"/> Weekly summary email</label></div><div className="card"><h3>Sensor Integration</h3><label>Sensor API endpoint<input defaultValue="wss://api.calmy.health/sensors/live"/></label><label>Backend API endpoint<input defaultValue="https://api.calmy.health/v1"/></label><small>For production, put these values in environment variables.</small></div><div className="card"><h3>Consent & Privacy</h3><p>{user.role==="Patient"?"You can request data export or withdraw research consent.":"Doctors can access only assigned patient records."}</p><button className="ghost-btn" onClick={()=>alert("Data export request created.")}>Request Data Export</button></div></div><button className="primary-btn" onClick={()=>{setSaved(true);setTimeout(()=>setSaved(false),2000)}}>Save Preferences</button>{saved&&<div className="toast">Settings saved.</div>}</div>
}
